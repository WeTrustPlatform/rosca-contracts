#!/bin/bash
# DIR is the directory of this script
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
$DIR/../../node_modules/ethereumjs-testrpc/build/cli.node.js -m "myth like bonus scare over problem client lizard pioneer submit female collect" &
$DIR/run-tests.sh